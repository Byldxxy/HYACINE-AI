// src/pet/main.jsx - 桌宠模式入口
import React from 'react';
import { createRoot } from 'react-dom/client';
import PetScene from './PetScene';

createRoot(document.getElementById('root')).render(<PetScene />);
